const fs = require("fs");

/*

fs.writeFileSync(
  "./sync.txt",
  "Created the file and insterted this text via fs.writeFile Sync function"
); // this is a sync call

fs.writeFile(
  "./async.txt",
  "Created the file and insterted this text via fs.writeFile Sync function",
  (err) => {}
); // Async

*/
